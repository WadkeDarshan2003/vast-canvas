rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEVELOPMENT MODE: All rules removed as requested
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
